# External systems hub

This page is the **index**: the diagram has four tiles (packs 1–4). Each pack keeps **≤4 sibling groups** with **≤4 nodes** per group.

Connector rows come from `integrationConnectors` in `data/dummyData.ts`; additional nodes mirror agent-side channels (FHIR, LIMS, imaging, LMS, MedDRA, GCP, …).
