# Full mesh (legacy)

The single-canvas mesh was replaced with functional domain maps so the architecture is readable at a glance.

Use **`mesh_hub.md`** or the root overview's **Functional agent domains** group.
