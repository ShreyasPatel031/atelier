# Clinical agents

Category slice of `systemAgents` in `data/dummyData.ts`.

The embedded diagram shows **only edges whose endpoints both lie in this category**. Cross-category links are on the **root overview** and in **mesh packs**.
