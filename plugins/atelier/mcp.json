{
  "$schema": "https://cursor.com/schemas/mcp.json",
  "mcpServers": {
    "atelier": {
      "type": "stdio",
      "command": "npx",
      "args": ["-y", "@atelier-inc/atelier-mcp@latest"],
      "env": {
        "ATELIER_LOCAL_REPO_ROOT": "${workspaceFolder}/demo/repos"
      },
      "description": "Browse and live-edit architecture diagrams — overview, modules, and drill-down views for any indexed repository"
    }
  }
}
